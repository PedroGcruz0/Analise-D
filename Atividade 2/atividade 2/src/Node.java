public class Node {
    int data;
    Node next;

    /**
     * Construtor para criar um novo nó.
     * @param data O valor inteiro a ser armazenado no nó.
     */
    public Node(int data) {
        this.data = data;
        this.next = null;
    }
}